apiVersion: v1
kind: Service
metadata:
 name: todolist-nodeport-service
 namespace: matepp
spec:
 type: NodePort
 selector:
   app: todolist
 ports:
   - protocol: TCP
     port: 80
     targetPort: 8080
     nodePort: 30007
